# T017: Update ImportExportModal Usage in page.tsx

**Type**: FE (Props Cleanup)
**Priority**: Low - simplifies component usage
**State**: pending

---

## Business Summary

Remove modal state props from page.tsx ImportExportModal usage.

---

## Logic

Remove isOpen and onClose props. Component self-manages from store.

---

## Technical Logic

Update ImportExportModal usage (app/page.tsx:123-126):

Before:
```typescript
<ImportExportModal
  isOpen={isImportExportOpen}
  onClose={() => setIsImportExportOpen(false)}
/>
```

After:
```typescript
<ImportExportModal />
```

Ensure button in page.tsx calls store action (should already be done in T008):
```typescript
<Button onClick={() => useUiStore.getState().openImportExport()}>
  <ImportExportIcon />
</Button>
```

---

## Testing

Manual verification:
- Import/export button opens modal
- Modal closes
- Functionality works

---

## Files

- `app/page.tsx` (modify)

---

## Patterns

Props elimination.

---

## Dependencies

- **Blocks**: None
- **Blocked By**: T016

---

## Acceptance Criteria

- [ ] Modal props removed
- [ ] Button calls store action
- [ ] Modal functional
