# T020: Unit Tests for `FilterChips` Component

**Status**: `pending`

## Action
Create

## Business Summary
Verify filter chip component behavior

## Testing
Test cases:
- Chips appear only when filter is active (non-default)
- Search chip shows when searchQuery is not empty
- Tag chip shows when selectedTag is not "all"
- Sort chip shows when sortKey is not "newest"
- Each chip shows correct label/value
- Remove button calls correct callback
- All chips render when multiple filters active
- No chips render when all filters are default
- Keyboard accessible (Enter/Space triggers remove)

## Files
- **Create**: `components/bookmarks/__tests__/FilterChips.test.tsx`

## Dependencies
- Requires T003 (`FilterChips`) to be implemented first
