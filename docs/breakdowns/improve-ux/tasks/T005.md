# T005: Unit Tests for `useBookmarkSelection`

**Status**: `pending`

## Action
Create

## Business Summary
Verify selection hook logic correctness

## Testing
Test cases:
- `toggle` adds ID to Set if not present
- `toggle` removes ID from Set if present
- `selectAll` replaces Set with provided IDs
- `clearSelection` empties the Set
- `isSelected` returns correct boolean
- `selectedCount` returns Set.size
- Empty array handled correctly
- Single ID selection works
- All selected state works
- Partial selection works

## Files
- **Create**: `hooks/__tests__/useBookmarkSelection.test.ts`

## Dependencies
- Requires T001 (`useBookmarkSelection`) to be implemented first
