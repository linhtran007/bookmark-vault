# T017: Update Empty States with Clearer Copy

**Status**: `pending`

## Action
Modify

## Business Summary
Improve empty state messaging to be more actionable

## Logic
- "No bookmarks yet" → "No bookmarks yet. Add your first to get started."
- "No results found" → "No bookmarks match your filters. Try different filters or clear all."
- Ensure "Clear filters" button calls `clearAllFilters()`

## Technical Logic
```tsx
// No bookmarks state
<EmptyState
  title="No bookmarks yet"
  description="Add your first bookmark to get started."
  actionLabel="Add your first bookmark"
  onAction={onAddBookmark}
/>

// No results state
<EmptyState
  title="No results found"
  description="No bookmarks match your filters. Try different filters or clear all."
  actionLabel="Clear all filters"
  onAction={clearAllFilters}
/>
```

## Testing
Visual check:
- Copy is shorter and more actionable
- "Clear all filters" button resets all filters
- Action buttons work correctly

## Files
- **Modify**: `components/bookmarks/BookmarkListView.tsx`

## Dependencies
- Requires T012 (`clearAllFilters` wiring) to be implemented first
