# T019: Make Toolbar Sticky (Optional Polish)

**Status**: `pending`

## Action
Modify

## Business Summary
Keep toolbar accessible while scrolling long bookmark lists

## Logic
- Add `sticky top-0 z-10` to toolbar container
- Add background color with backdrop blur for readability
- Ensure it doesn't cover content (add spacing if needed)
- Mobile-friendly (works on small screens)

## Technical Logic
```tsx
<div className="sticky top-0 z-10 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
  <BookmarkToolbar ... />
</div>
```

## Testing
Visual check:
- Scroll down → toolbar stays at top
- Toolbar doesn't jump when content loads
- Content visible behind toolbar has blur effect
- Works on mobile (doesn't cover content)
- No z-index conflicts with other elements

## Files
- **Modify**: `components/bookmarks/BookmarkListView.tsx`

## Patterns
Standard sticky positioning with backdrop blur
