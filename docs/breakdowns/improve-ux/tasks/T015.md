# T015: Apply `MarqueeText` to Bookmark Card Titles

**Status**: `pending`

## Action
Modify

## Business Summary
Wrap long bookmark titles with marquee component

## Logic
- Replace title `<h3>` with `<MarqueeText>` wrapper
- Maintain existing truncation styling as base
- Keep title styling (font, weight, color)

## Technical Logic
```tsx
<MarqueeText>
  <h3 className="truncate">{bookmark.title}</h3>
</MarqueeText>
```

## Testing
Visual check:
- Short titles: no change, no animation
- Long titles: truncated by default, marquee on hover
- Reduced motion: underline instead of marquee
- Styling matches previous title appearance

## Files
- **Modify**: `components/bookmarks/BookmarkCard.tsx`

## Dependencies
- Requires T013 (`MarqueeText`) to be implemented first
