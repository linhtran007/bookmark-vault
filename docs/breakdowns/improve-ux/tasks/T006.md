# T006: Unit Tests for `bulkDelete`

**Status**: `pending`

## Action
Create

## Business Summary
Verify bulk delete behavior in useBookmarks hook

## Testing
Test cases:
- Deletes multiple bookmarks successfully
- Shows correct toast with count
- Handles partial failures (some succeed, some fail)
- Handles empty array gracefully
- Optimistic UI updates correctly (bookmarks removed immediately)
- Toast error shown on failure

## Files
- **Create**: `hooks/__tests__/useBookmarks.bulkDelete.test.ts`

## Dependencies
- Requires T002 (`bulkDelete` in useBookmarks) to be implemented first
